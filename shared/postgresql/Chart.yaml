apiVersion: v2
name: ollama
description: Chart pointing to local ollama chart template
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: postgresql
    version: 1.0.0
    repository: "file://../../charts/postgresql"
    condition: postgresql.enabled